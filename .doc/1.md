先说结论

BigBanana 里的“剧本完善”本质上不是“文学润色”，而是把原始故事转成一套可驱动后续 角色/场景/道具/分镜/关键帧/视频 的生产级结构。
它实际是一条三段式流水线：结构化解析 → 视觉补全 → 分镜生成/纠偏，入口在 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/components/StageScript/index.tsx:557。
你们 AIGCmanju 里已经把这套思路收敛成了显式的 /refine 能力，只是 schema 更偏“视频生产流水线”，见 /Users/xingyi/Downloads/0000主任务0000/AIGCmanju/aigc-server/src/main/java/com/example/aigc/service/ScriptWorkflowService.java:61。
BigBanana 的“剧本完善”逻辑

第 1 步：结构化剧本
parseScriptStructure 会让模型从原始文本里抽出 title / genre / logline / characters / scenes / props / storyParagraphs，并强制只返回 JSON，位置在 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:48。
第 2 步：结果标准化
代码会补默认 id、修正道具分类、纠正 sceneRefId，如果模型没给 storyParagraphs，就从原文段落兜底补齐，核心在 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:94 和 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:219。
第 3 步：统一视觉世界观
在补角色/场景提示词前，它会先生成一个全局 Art Direction Brief，把色板、光影、材质、人物比例、统一风格锚点先定死，位置在 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/visualService.ts:34。
第 4 步：补角色/场景/道具视觉提示词
角色优先走“批量统一风格生成”，失败再逐个生成；场景 prompt 明确要求“纯环境、不能出现人物”；道具 prompt 明确要求“物体单独出镜”，见 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/visualService.ts:153 和 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/visualService.ts:289。
第 5 步：按时长规划分镜数
generateShotList 会先根据总时长和单镜头基准时长，算出总镜头数，再平均分到各场景，位置在 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:981。
第 6 步：段落映射到场景
它会先用 sceneRefId 直连；如果没有，再做语义匹配；再不行就借邻近场景段落；最后再全局兜底，这一步很像“剧本完善后的可执行映射层”，见 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:1005。
第 7 步：生成分镜并自动纠偏
每个场景都要求返回 精确数量 的 shots；如果数量不对，就走 shotRepair 二次纠偏；还不够就用兜底镜头补满，见 /Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:1213、/Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:1317、/Users/xingyi/Downloads/0000主任务0000/BigBanana-AI-Director-feature_prompttemplate/services/ai/scriptService.ts:1355。
第 8 步：质量校验 + 自动修补
它会检查 actionSummary / cameraMovement / shotSize / keyframes 是否完整，不够就自动改写补齐，所以它不是“只生成”，而是“生成后修复”，逻辑就在同文件的质量管线附近，最终在 shot 上挂 qualityAssessment。
它的提示词思路，压缩成一句话

不是“帮我把剧本写好看”，而是：请把原始文本整理成可被 AI 视频生产链条直接消费的结构化叙事资产。
BigBanana 风格的提示词特征

强约束输出：几乎每一步都要求 Output ONLY valid JSON。
中间结构很明确：先产 ScriptData，再产 ArtDirection，再产 ShotList。
语言与控制分离：很多 prompt 主体是英文控制语，但明确要求最终内容用指定语言输出。
强调一致性：统一风格锚点会被注入到角色、场景、分镜里。
强调生产可执行性：不是抽象分析，而是要能直接喂给下游图像/视频生成。
如果你要总结成“剧本完善 prompt”的核心要求

角色定位：影视编剧 + 分镜策划师 + AIGC 视频制作顾问。
目标：把原始剧本整理成适合视频生成的结构化脚本，而不是文学改写。
输出：严格 JSON，不要解释、不要 Markdown。
内容：至少有标题、摘要/一句话梗概、角色、场景/背景、道具、分段。
关联：每个分段必须绑定角色、背景、道具，并给出动作摘要和镜头建议。
约束：所有实体有唯一 id，所有描述用指定语言，字段稳定可解析。
我帮你提炼的“BigBanana 风格剧本完善提示词”

System Prompt
你是一名专业的影视编剧、分镜策划师和 AIGC 视频制作顾问。
你的任务不是做文学润色，而是把用户原始剧本整理成适合视频生产流水线消费的结构化脚本。
你需要抽取角色、场景、道具、剧情分段，并让每个分段都能直接支撑后续分镜、关键帧和视频生成。
输出必须是严格 JSON，不要输出解释，不要输出 Markdown。
所有描述使用 {{language}}。
所有实体都必须有稳定唯一的 id。
User Prompt（偏 BigBanana 原始结构）
项目信息：项目名 {{projectName}}，风格 {{visualStyle}}，目标时长 {{targetDuration}}，语言 {{language}}
请分析下面原始剧本，并输出结构化 JSON，至少包含：
title
genre
logline
characters：id / name / gender / age / personality
scenes：id / location / time / atmosphere
props：id / name / category / description
storyParagraphs：id / text / sceneRefId
要求：
每段剧情都尽量绑定到具体场景；
道具只保留对叙事或镜头复用有意义的 recurring props；
输出必须是严格 JSON。
User Prompt（偏你们 AIGCmanju 当前 refine 结构）
项目信息：项目名 {{projectName}}，风格 {{visualStyle}}，比例 {{aspectRatio}}，时长 {{targetDuration}} 秒，语言 {{language}}
请将下面原始剧本整理为适合视频生成流水线消费的 JSON，至少包含：
title
summary
characters：每项含 id / name / description
backgrounds：每项含 id / name / description
props：每项含 id / name / description
scenes
segments：每项含 id / title / scriptText / actionSummary / cameraMovement / characterIds / backgroundIds / propIds
要求：
segments 必须能直接作为视频片段输入；
每个分段聚焦一个可拍摄动作单元；
输出必须是严格 JSON。
你们项目里已经落地的 refine prompt

system 模板在 /Users/xingyi/Downloads/0000主任务0000/AIGCmanju/aigc-server/src/main/resources/prompts/script/refine-system.md:1
user 模板在 /Users/xingyi/Downloads/0000主任务0000/AIGCmanju/aigc-server/src/main/resources/prompts/script/refine-user.md:1
调用拼装逻辑在 /Users/xingyi/Downloads/0000主任务0000/AIGCmanju/aigc-server/src/main/java/com/example/aigc/service/ScriptWorkflowService.java:325
一个很关键的判断

如果你想“更像 BigBanana”，就保留 genre / logline / storyParagraphs / sceneRefId / ArtDirection 这套中间层。
如果你想“更像生产系统”，你们 AIGCmanju 现在这套 summary / backgrounds / segments 已经更接近可落地的视频流水线。
如果你愿意，我下一步可以继续帮你做两件很实用的事之一：

把上面的总结直接整理成一版 可落地的 refine-system.md + refine-user.md 最终版
或者帮你做一版 “BigBanana 风格 → AIGCmanju schema” 的字段映射表