项目：{{projectName}}
视觉风格：{{visualStyle}}
语言：{{language}}

下面是当前**完整结构化剧本 JSON**（请以其为唯一权威来源，按 id 合并增强）：
{{structuredScriptJson}}

原文摘录（供核对语气与事实，勿编造新情节）：
{{originalExcerpt}}

请仅输出 JSON 对象，顶层键为 `scenes` 与 `segments`，数组元素与输入 id 对齐，并填入拍摄向字段。
