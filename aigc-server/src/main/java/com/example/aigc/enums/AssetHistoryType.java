package com.example.aigc.enums;

/**
 * 数字资产生成历史类型（与 asset_generation_history.asset_type 一致）。
 */
public enum AssetHistoryType {
    KEYFRAME,
    TURNAROUND,
    STORYBOARD,
    THREE_VIEW,
    STORYBOARD_CROP,
    GROUP_SCENE,
    VIDEO
}
