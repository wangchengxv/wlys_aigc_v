# Miioo Project

## Structure
- `frontend/`: React 18 + TypeScript
- `backend/`: Spring Boot 3
- `SPECS/` and `DOCS/`: baseline docs

## Start Dependencies
```bash
docker-compose up -d
```

## Run
```bash
cd backend && mvn spring-boot:run
cd frontend && npm install && npm run dev
```
