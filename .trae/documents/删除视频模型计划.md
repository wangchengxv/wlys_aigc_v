# 删除所有视频模型计划

## 任务概述
删除系统中所有视频模型（包括仅视频和图+视频能力的模型）

## 待删除的视频模型清单

### Java 端 (PresetModelRegistry.java)
需要删除以下行：
- **第 21 行**: `new PresetModel("onelinkai", "MiniMax-M2.1"...` (video)
- **第 22 行**: `new PresetModel("onelinkai", "video-kling-v3-6"...` (video)
- **第 23 行**: `new PresetModel("onelinkai", "kling-v1"...` (video)
- **第 24-25 行**: `viduq3-turbo`, `viduq3-pro` (video)
- **第 26-27 行**: `image-vidu-q2-fast`, `image-vidu-q2` (image+video)
- **第 28-31 行**: `viduq2-turbo`, `viduq2`, `viduq1`, `viduq1-classic` (image+video)
- **第 32 行**: `vidu2.0` (video)
- **第 50-51 行**: 豆包视频 Seedance 2.0 系列 (video)
- **第 52 行**: Moark Wan2.1 图生视频 (video)
- **第 53-61 行**: `vidu` 提供商的所有模型 (video/image+video)
- **第 62-71 行**: `vidu_onelink` 提供商的所有模型 (video/image+video)
- **第 72-75 行**: `kling` 提供商的所有模型 (video)

### Go 端 (preset_models.go)
需要删除以下行：
- **第 24 行**: MiniMax-M2.1
- **第 25-33 行**: `vidu` 提供商的所有模型
- **第 34-42 行**: `vidu_onelink` 提供商的所有模型
- **第 43-46 行**: `kling` 提供商的所有模型
- **第 63-65 行**: ark 豆包视频 + moark

## 执行步骤

1. **修改 Java 文件**: 删除所有包含 "video" 能力的 PresetModel 条目
2. **修改 Go 文件**: 删除所有包含 "video" 能力的 PresetModel 条目
3. **验证编译**: 运行 Java 和 Go 项目的编译检查确保无错误

## 保留的模型（文本和图像）
- 所有 `text` 能力的模型
- 所有 `image` 能力的模型（如 wanx-v1, kling 图像生成, seedream 等）

## 注意事项
- 删除后系统将无法使用视频生成功能
- 确保没有任何业务逻辑依赖这些视频模型
- 建议在执行前备份相关文件
