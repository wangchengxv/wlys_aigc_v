# 项目详情页「项目工作区」功能卡片移位计划

## Summary
- 目标：将 `ScriptProjectDetailPage` 中的「项目工作区」功能卡片（`ScriptProjectWorkflowNav`）从当前主区域最上方，移动到「成果封面」区块下方。
- 结果预期：页面视觉顺序变为「成果封面」→「项目工作区」→「项目详情面板/进度等下方功能区」。
- 范围：仅调整组件在 JSX 中的位置，不改动业务逻辑、数据绑定、路由结构与样式命名。

## Current State Analysis
- 目标文件：`/Users/xingyi/Downloads/AIGC_小云雀/AIGC_university_副本/aigc-site-react/src/pages/ScriptProjectDetailPage.tsx`
- 当前结构：
  - 外层：`<div className="script-project-workflow-layout">`
  - 当前位置：`<ScriptProjectWorkflowNav projectId={projectId} />` 在 `script-project-workflow-layout__main` 之前（即主内容区顶部外侧）。
  - 主内容区内顺序：`delivery-hero`（成果封面）→ `ConfirmDialog` → `FixedPanelDock`（项目详情面板）→ `PipelineProgressBar`。
- 组件来源确认：
  - `ScriptProjectWorkflowNav` 定义于 `src/components/script/ScriptProjectWorkflowNav.tsx`，是「项目工作区」卡片本体。

## Proposed Changes
- 文件：`/Users/xingyi/Downloads/AIGC_小云雀/AIGC_university_副本/aigc-site-react/src/pages/ScriptProjectDetailPage.tsx`
- 变更 1（What）：将 `<ScriptProjectWorkflowNav projectId={projectId} />` 从当前位置移除。
  - Why：当前位于你标注的“1”位置，不符合期望。
  - How：删除 `return` 结构中位于 `script-project-workflow-layout` 下、`script-project-workflow-layout__main` 之前的该组件节点。
- 变更 2（What）：将同一组件插入到 `delivery-hero` 区块结束标签 `</div>` 之后。
  - Why：对应你标注的“2”位置（成果封面下方），并保证其他功能区继续在下方。
  - How：在 `<section className="script-detail-page delivery-showcase-page">` 中，`delivery-hero` 后、`ConfirmDialog` 前插入 `<ScriptProjectWorkflowNav projectId={projectId} />`。
- 非变更项：
  - 不改 `ScriptProjectWorkflowNav` 组件内部内容。
  - 不改 `FixedPanelDock`、`PipelineProgressBar`、`ConfirmDialog` 逻辑。
  - 不改样式类名与样式文件。

## Assumptions & Decisions
- 决策 1：按你截图语义执行“整体移动”，不拆分或重构卡片内容。
- 决策 2：保持下方功能区（项目详情面板、进度条等）相对顺序不变，仅让「项目工作区」插入到成果封面之后。
- 假设 1：无需同步修改其他项目子页面（仅本页 `ScriptProjectDetailPage`）。
- 假设 2：当前样式间距可接受；若出现轻微间距问题，再做二次样式微调（本次先不包含）。

## Verification Steps
- 代码级检查：
  - 确认 `ScriptProjectDetailPage.tsx` 中 `ScriptProjectWorkflowNav` 仅出现 1 次。
  - 确认该组件位于 `delivery-hero` 后、`ConfirmDialog` 前。
- 页面级检查：
  - 进入任一项目详情页 `/script-projects/:projectId`。
  - 验证视觉顺序为：成果封面（含项目摘要）→ 项目工作区卡片 → 项目详情面板及其余功能区。
- 回归检查：
  - 验证「项目工作区」内各 tab 跳转仍正常（总览/剧本/资产/视频/配音/口型/剪辑/导出/审计）。
