- [x] Java 服务商目录与模型配置链路可识别 Vidu 图生视频能力
- [x] Java 后端将 Vidu 图生视频请求提交到 `POST //vidu/vidu/ent/v2/img2video`
- [x] Java 后端请求头使用 `Authorization: Token {apiKey}` 与 `Content-Type: application/json`
- [x] Java 后端对 `images` 单图输入执行必要校验（格式、比例、体积或等价约束）
- [x] Java 后端按模型族对 `duration` 与 `resolution` 执行约束校验
- [x] Java 后端正确处理 `audio`、`audio_type`、`voice_id` 的条件生效关系
- [x] Java 后端正确处理 `is_rec`、`off_peak`、`watermark` 等可选字段
- [x] 参数冲突场景返回明确 4xx 业务错误
- [x] Java 后端可查询 Vidu 任务状态并映射处理中/成功/失败
- [x] Java 后端成功结果可同时保留主视频 URL 与水印 URL（去重）
- [x] Java 后端失败结果可映射统一失败结构并保留供应商失败原因
- [x] Java 自动化测试覆盖核心场景与异常场景并通过
- [x] 非 Vidu 供应商路径不回归
