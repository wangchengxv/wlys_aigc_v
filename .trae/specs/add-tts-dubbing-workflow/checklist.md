- [x] 剧本工程可以创建并持久化配音任务，且任务至少关联项目、镜头、文本、状态与结果音频文件
- [x] 配音任务支持失败原因记录与单任务重试
- [x] 配音结果音频通过统一媒体资源抽象保存，并带有 provider / bucket / object key / publicUrl 元数据
- [x] 用户可以为项目保存并读取 TTS 模型与基础音色参数
- [x] 前端存在单独的配音管理页，可发起生成、查看状态、试听结果、重试失败任务
- [x] 导出页可以提示配音是否已准备完成，并提供跳转入口
- [x] 后端测试覆盖配音工作流关键场景且通过
- [x] 前端构建通过，新增页面与路由无类型错误

汇总：
- 已补齐项目级配音页、工作流入口、项目导航与导出页配音准备提示。
- 已在前端 `types` / `api` / `store` 中接入配音任务、TTS 模型设置、音色语言语速与轮询刷新。
- 已执行 `aigc-site-react` 前端构建，确认新增页面、路由与类型定义可通过。
