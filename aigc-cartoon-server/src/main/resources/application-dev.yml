spring:
  config:
    import: application.yml

  datasource:
    url: jdbc:mysql://localhost:3306/newaigc?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: rootroot

logging:
  level:
    com.aigc.cartoon: debug
